import BookCard from '../components/BookCard';
import Field from '../components/Field';

export default function BooksPage({ books, book, setBook, onSubmit, onRemove }) {
  return <section className="books-page"><div className="section-heading"><div><p className="eyebrow">COLLECTION / BOOKS</p><h2>The collection</h2></div><span className="count">{books.length} titles</span></div><div className="books-layout"><div className="book-grid">{books.map((item) => <div className="book-card-wrap" key={item._id}><BookCard title={item.title} author={item.author} category={item.category} available={item.available} /><button className="text-action book-remove" onClick={() => onRemove(item._id)}>Remove from collection</button></div>)}{!books.length && <p className="muted">Add the first title to begin your collection.</p>}</div><div className="form-card"><p className="eyebrow">ADD TO COLLECTION</p><form onSubmit={onSubmit}><Field label="Title" name="title" value={book.title} onChange={(e) => setBook({ ...book, title: e.target.value })} /><Field label="Author" name="author" value={book.author} onChange={(e) => setBook({ ...book, author: e.target.value })} /><Field label="Category" name="category" value={book.category} onChange={(e) => setBook({ ...book, category: e.target.value })} /><Field label="ISBN" name="isbn" value={book.isbn} onChange={(e) => setBook({ ...book, isbn: e.target.value })} /><button className="submit">Add book</button></form></div></div></section>;
}
